最终目标：

* 本地彻底重写并复现ragen框架。我认为现有项目的结构不合理，希望对其代码进行深度优化。但同时保留其原框架的所有功能，指标。

项目内代码具体要求：

* 编码高度工程化，鲁棒性强，基于transformers，pytorch等库编写。将项目解耦为多个相对独立的模块，模块之间高度解耦，可替换性强。以下是关于解耦的详细说明：
  * 项目针对的是多轮次强化学习场景，其至少可以独立分为：
    * 环境模块：只负责环境的定义，（一般一个环境一个py文件，一个类，下面的其他模块，agent，RL算法等等也同理）。在这里可以定义一个通用的环境模版，后续每一个新环境都得遵守该模版的范式。环境可以就是基于gymnasium库（不是老版本的gym库，gym可以用作gymnasium的别名）定义，也可以是一些其他更常见的任务，例如解数学题，编程问题等等。但是我希望无论是传统的游戏交互场景，还是新兴的基于大模型的各种nlp逻辑推理任务，最好都能有一个较为统一的规范。
      * 在你最初的生成阶段，可以只引入两三个简单环境：解一道数学题/编程题，cartpole，frozenlake等等
    * agent模块：只负责智能体的定义，设计智能体与外界交互的各种接口。模型选择应该都仅限于纯文本LLM了，它们都可以基于transformers库导入。agent模块里最核心的类接口就是文本输入文本输出接口，一般定义为某个类的chat_request方法。这里还可以通过一个if语句，将chat_request接口兼容到远程api调用中来（远程api仅限于纯测试不训练的场景），api调用采用openai库的规范。agent模块同样应该存在一个统一模版，后续可以引入许多不同的模型，构造出不同的agent来进行训练/比较。
      * 在你最初的生成阶段，可以只引入两个简单模型，固定从hf上下载。你可以先帮我挑选两个合适的模型
    * RL算法模块：只负责RL算法的定义。定义一个通用的RL算法模版，尽量保留相对统一的对外接口。
      * 在你最初的生成阶段，可以只提供几个基础算法。例如dqn，ddpq，td3，ppo这四个。
    * ragen框架模块，这篇论文最核心的贡献，即位于RL算法上层的顶级框架。框架只有一个，但论文内部可能给出了不同实现，假设有一个快速版和慢速版，那么我也可以通过一个变量去进行切换。
    * configs模块。用来放置整个项目中所有非敏感的配置项。包括但不限于：环境相关选择，agent选择，RL算法选择，ragen框架各种参数的调优，等等。在这个文件夹里可以包含多个配置py文件，目标是使得逻辑更清晰。必要时可以引入（嵌套）字典等数据结构，甚至必要时可以用dataclass，目的就是加强逻辑性。
    * utils模块。这个我已经给你提供好了，你只需要去使用即可。核心就是logger.py，你需要在项目里的每一个角落都给到足够的日志提示，以便后续调试debug。
  * 以上基本就是对解耦各模块的说明。核心就是希望项目做到调试灵活，方便切换，方便后续在实验的时候控制单一变量。例如假设我现在使用环境A，模型A，算法A，框架参数A成功跑通并完成了试验参数的测量，后续我可以直接通过一个变量值的修改，例如将模型A切换到模型B，继续跑通实验并开始新的一轮测量。而RL算法，ragen框架，环境这些维度的切换也是同理。
  * 最后一点，项目各处附上详细到位的中文注释。最后生成一份非常详细的readme.md文档

